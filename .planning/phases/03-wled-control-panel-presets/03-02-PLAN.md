---
phase: "03"
plan: "03-02"
title: "Power + Brightness Controls"
type: tdd
wave: 2
depends_on: ["03-01"]
estimated_minutes: 40
requirements: ["CTRL-01", "CTRL-02"]
---

# Plan 03-02: Power + Brightness Controls

## Objective
Build the power on/off toggle and brightness slider UI components that dispatch through WLEDControlService. These are the first visible controls for the WLED control panel.

## Context
- @src/core/wled/WLEDControlService.ts — from Plan 03-01
- @src/core/store/cubeStateStore.ts — on, brightness state
- @src/components/ui/ — shadcn components available

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Install shadcn components + react-colorful, build PowerBrightnessPanel</name>
  <files>
    src/control/PowerBrightnessPanel.tsx
    src/control/__tests__/PowerBrightnessPanel.test.tsx
  </files>
  <behavior>
    - Test: Renders a power toggle switch reflecting cubeStateStore.on
    - Test: Clicking toggle calls WLEDControlService.setPower with toggled value
    - Test: Renders brightness slider with current cubeStateStore.brightness value
    - Test: Changing slider value calls WLEDControlService.setBrightness
    - Test: Brightness slider shows numeric value label (0-255)
    - Test: Toggle and slider have min 44px touch target height
  </behavior>
  <action>
    First, install shadcn components needed for Phase 3:
    ```bash
    npx shadcn@latest add slider switch scroll-area tabs popover tooltip separator
    npm install react-colorful
    ```
    
    Create PowerBrightnessPanel component:
    - shadcn Switch for power toggle (wrap in 44px touch target)
    - shadcn Slider for brightness (0-255, step 1)
    - Display brightness value as text next to slider
    - Use cubeStateStore selectors for current values
    - On change: call WLEDControlService methods
    - Debounce brightness slider at 100ms to avoid flooding
    - Layout: vertical stack with labels
  </action>
  <verify>
    <automated>npx vitest run src/control/__tests__/PowerBrightnessPanel.test.tsx</automated>
  </verify>
  <done>Power toggle and brightness slider render with correct state and dispatch WLED updates</done>
</task>

## Verification
```bash
npx vitest run src/control/__tests__/PowerBrightnessPanel.test.tsx
```

## Must-Haves
- [ ] Power toggle reflects and changes on/off state
- [ ] Brightness slider reflects and changes brightness (0-255)
- [ ] Changes dispatch through WLEDControlService (not direct API calls)
- [ ] Touch targets >= 44px
