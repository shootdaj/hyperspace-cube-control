---
phase: "03"
plan: "03-04"
title: "Palette Browser"
type: tdd
wave: 2
depends_on: ["03-01"]
estimated_minutes: 35
requirements: ["CTRL-04"]
---

# Plan 03-04: Palette Browser

## Objective
Build a scrollable palette browser that fetches all WLED palettes and lets the user apply one with a single tap.

## Context
- @src/core/store/effectPaletteStore.ts — from Plan 03-03, has palettes array + fetchPalettes
- @src/core/wled/WLEDControlService.ts — setPalette(index)
- @src/core/store/cubeStateStore.ts — paletteIndex

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: PaletteBrowser component</name>
  <files>
    src/control/PaletteBrowser.tsx
    src/control/__tests__/PaletteBrowser.test.tsx
  </files>
  <behavior>
    - Test: Renders list of palette names from effectPaletteStore
    - Test: Active palette (matching cubeStateStore.paletteIndex) is highlighted
    - Test: Clicking a palette calls WLEDControlService.setPalette with the palette's index
    - Test: Search input filters palette list by name (case-insensitive)
    - Test: Each palette item has min 44px touch target
  </behavior>
  <action>
    Create PaletteBrowser component (mirrors EffectBrowser pattern):
    - Input field for search/filter
    - ScrollArea wrapping palette list
    - Each item: palette name, highlighted if active
    - On click: WLEDControlService.setPalette(index)
    - List items: min-h-11 (44px) with py-2 px-3
    - Use effectPaletteStore.palettes (already fetched in 03-03)
  </action>
  <verify>
    <automated>npx vitest run src/control/__tests__/PaletteBrowser.test.tsx</automated>
  </verify>
  <done>Palette browser displays all palettes, supports search, applies palette on tap</done>
</task>

## Verification
```bash
npx vitest run src/control/__tests__/PaletteBrowser.test.tsx
```

## Must-Haves
- [ ] All WLED palettes listed by name
- [ ] Active palette visually distinguished
- [ ] One-tap application through WLEDControlService
- [ ] 44px+ touch targets
