---
phase: "03"
plan: "03-07"
title: "Responsive Layout"
type: standard
wave: 4
depends_on: ["03-02", "03-03", "03-04", "03-05", "03-06"]
estimated_minutes: 45
requirements: ["UI-03", "UI-04", "UI-06"]
---

# Plan 03-07: Responsive Layout

## Objective
Assemble all control components into a responsive layout: stacked tabs on mobile (375px), multi-panel sidebar on desktop (1440px). Wire up state sync, effect/palette fetching, and integrate the control panel into App.tsx.

## Context
- @src/App.tsx — current app shell with 3D cube
- @src/control/PowerBrightnessPanel.tsx — from 03-02
- @src/control/EffectBrowser.tsx — from 03-03
- @src/control/PaletteBrowser.tsx — from 03-04
- @src/control/SpeedIntensityPanel.tsx — from 03-05
- @src/control/ColorPickerPanel.tsx — from 03-05
- @src/control/PresetPanel.tsx — from 03-06

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: ControlPanel container with responsive layout</name>
  <files>
    src/control/ControlPanel.tsx
    src/control/__tests__/ControlPanel.test.tsx
    src/App.tsx
  </files>
  <behavior>
    - Test: ControlPanel renders all sub-panels (power, effects, palettes, speed/intensity, colors, presets)
    - Test: On mobile viewport (< 768px), tabs are visible for panel switching
    - Test: On desktop viewport (>= 768px), panels are stacked in sidebar
    - Test: Tab switching shows correct panel content on mobile
    - Test: All interactive elements have min 44px touch target
  </behavior>
  <action>
    Create ControlPanel as the top-level control container:
    
    Mobile (< md):
    - shadcn Tabs at top: "Controls" | "Effects" | "Palettes" | "Presets"
    - "Controls" tab: PowerBrightnessPanel + SpeedIntensityPanel + ColorPickerPanel
    - "Effects" tab: EffectBrowser
    - "Palettes" tab: PaletteBrowser
    - "Presets" tab: PresetPanel
    - Full width, scrollable
    
    Desktop (>= md):
    - All panels stacked vertically in a scrollable sidebar
    - Sections separated with shadcn Separator
    - Section headers for visual hierarchy
    
    Update App.tsx:
    - Layout: `flex flex-col md:flex-row`
    - 3D cube: `flex-1` on desktop, `h-[40vh]` on mobile
    - ControlPanel: `w-full md:w-[400px] md:max-w-[400px]` sidebar on desktop
    - On mount (when connected): fetch effects + palettes from WLEDRestClient
    - Start state sync (WLEDStateSync) alongside live sync
    - Load presets from localStorage
  </action>
  <verify>
    <automated>npx vitest run src/control/__tests__/ControlPanel.test.tsx</automated>
  </verify>
  <done>Responsive control panel with tab navigation on mobile and sidebar on desktop</done>
</task>

<task type="auto">
  <name>Task 2: Integration test — full control round-trip</name>
  <files>
    test/integration/control-panel.test.tsx
  </files>
  <action>
    Integration test using virtualCube mock server:
    1. Render App with wizard completed
    2. Verify effects and palettes are fetched and populated
    3. Toggle power → verify REST call made with {on: ...}
    4. Change brightness slider → verify REST call
    5. Select an effect → verify REST call with {seg: [{fx: ...}]}
    6. Save a preset → verify it appears in preset list
    7. Load the preset → verify REST calls match saved state
    
    Use MSW virtual cube handlers from test/mocks/virtualCube.ts.
    Test with the server intercepting all API calls.
  </action>
  <verify>
    <automated>npx vitest run test/integration/control-panel.test.tsx</automated>
  </verify>
  <done>Full round-trip integration test passes with virtual cube</done>
</task>

## Verification
```bash
npx vitest run src/control/__tests__/ControlPanel.test.tsx test/integration/control-panel.test.tsx
```

## Must-Haves
- [ ] Mobile: tabbed interface at 375px with all controls reachable
- [ ] Desktop: sidebar layout at 1440px with all controls visible
- [ ] 3D cube visible alongside controls on both layouts
- [ ] Effects and palettes fetched on connection
- [ ] State sync active for real-time updates
- [ ] All touch targets >= 44px
