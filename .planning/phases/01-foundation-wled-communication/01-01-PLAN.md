---
phase: 01-foundation-wled-communication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - vitest.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - src/index.css
  - src/main.tsx
  - src/App.tsx
  - src/test-setup.ts
  - src/components/ui/.gitkeep
  - src/core/pipeline/.gitkeep
  - src/core/store/.gitkeep
  - src/core/wled/.gitkeep
  - src/plugins/inputs/.gitkeep
  - src/plugins/outputs/.gitkeep
  - src/plugins/mapping/.gitkeep
  - src/setup/.gitkeep
  - src/ui/.gitkeep
  - test/integration/.gitkeep
  - test/scenarios/.gitkeep
  - test/mocks/.gitkeep
  - .gitignore
  - index.html
autonomous: true
requirements:
  - DEP-01
  - DEP-02

must_haves:
  truths:
    - "`npm run dev` starts a Vite dev server without errors"
    - "`npm run build` produces a `/dist` folder without errors"
    - "`npx vitest run --dir src` runs (0 tests, no crash)"
    - "Tailwind v4 CSS-first config is active (`@import \"tailwindcss\"` in index.css)"
    - "shadcn/ui CLI has been initialized and at least one component (Button) is present in `src/components/ui/`"
    - "Directory structure for plugin architecture exists (core/, plugins/, setup/, ui/, test/)"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite config with @tailwindcss/vite plugin and @ alias"
      contains: "@tailwindcss/vite"
    - path: "vitest.config.ts"
      provides: "Vitest config with jsdom environment and test-setup"
      contains: "jsdom"
    - path: "src/index.css"
      provides: "Tailwind v4 CSS-first entry"
      contains: "@import \"tailwindcss\""
    - path: "src/test-setup.ts"
      provides: "Testing-library DOM matchers setup"
      contains: "@testing-library/jest-dom"
    - path: "src/components/ui"
      provides: "shadcn/ui component directory"
  key_links:
    - from: "vite.config.ts"
      to: "src/index.css"
      via: "@tailwindcss/vite plugin processes CSS"
      pattern: "tailwindcss"
    - from: "vitest.config.ts"
      to: "src/test-setup.ts"
      via: "setupFiles array"
      pattern: "setupFiles"
---

<objective>
Bootstrap the Vite + React 19 + TypeScript + Tailwind v4 + shadcn/ui project with all dev tooling configured. Establish the full directory skeleton for the plugin architecture.

Purpose: Every subsequent plan builds on this scaffold. Getting directory layout, build config, and test runner right here prevents structural refactoring later.
Output: A working npm project with `dev`, `build`, `lint`, and test scripts; Tailwind v4 CSS-first config; shadcn/ui initialized; directory skeleton matching the architecture spec.
</objective>

<execution_context>
@/Users/anshul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anshul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/01-foundation-wled-communication/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize Vite + React 19 + TypeScript project with Tailwind v4</name>
  <files>package.json, vite.config.ts, vitest.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, src/index.css, src/main.tsx, src/App.tsx, src/test-setup.ts, index.html, .gitignore</files>
  <action>
    The repo root already exists at `/Users/anshul/Anshul/Code/hyperspace-cube-control` with a git repo. Do NOT `npm create vite` — that would overwrite git history. Instead, create all files manually.

    1. Create `package.json` with these exact dependencies:
       ```json
       {
         "name": "hyperspace-cube-control",
         "private": true,
         "version": "0.1.0",
         "type": "module",
         "scripts": {
           "dev": "vite",
           "build": "tsc -b && vite build",
           "lint": "eslint .",
           "preview": "vite preview",
           "test": "vitest run --dir src",
           "test:integration": "vitest run --dir test/integration",
           "test:scenarios": "vitest run --dir test/scenarios",
           "test:all": "vitest run"
         },
         "dependencies": {
           "react": "^19.1.0",
           "react-dom": "^19.1.0",
           "zustand": "^5.0.12",
           "valtio": "^2.3.1"
         },
         "devDependencies": {
           "@types/node": "^22.0.0",
           "@types/react": "^19.1.0",
           "@types/react-dom": "^19.1.0",
           "@vitejs/plugin-react": "^4.5.0",
           "@tailwindcss/vite": "^4.1.0",
           "tailwindcss": "^4.1.0",
           "typescript": "~5.8.3",
           "vite": "^6.3.0",
           "vitest": "^3.1.0",
           "@testing-library/react": "^16.3.0",
           "@testing-library/jest-dom": "^6.6.0",
           "@testing-library/user-event": "^14.6.0",
           "msw": "^2.7.0",
           "jsdom": "^26.1.0",
           "@vitest/coverage-v8": "^3.1.0",
           "eslint": "^9.22.0",
           "@eslint/js": "^9.22.0",
           "typescript-eslint": "^8.26.0",
           "eslint-plugin-react-hooks": "^5.2.0",
           "eslint-plugin-react-refresh": "^0.4.19"
         }
       }
       ```

    2. Run `npm install` from the project root.

    3. Create `vite.config.ts`:
       ```typescript
       import path from 'path';
       import tailwindcss from '@tailwindcss/vite';
       import react from '@vitejs/plugin-react';
       import { defineConfig } from 'vite';

       export default defineConfig({
         plugins: [react(), tailwindcss()],
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
       });
       ```

    4. Create `vitest.config.ts`:
       ```typescript
       import path from 'path';
       import { defineConfig } from 'vitest/config';
       import react from '@vitejs/plugin-react';

       export default defineConfig({
         plugins: [react()],
         test: {
           environment: 'jsdom',
           globals: true,
           setupFiles: ['./src/test-setup.ts'],
           include: ['src/**/*.test.ts', 'src/**/*.test.tsx', 'test/**/*.test.ts', 'test/**/*.test.tsx'],
         },
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
       });
       ```

    5. Create `tsconfig.json`:
       ```json
       {
         "files": [],
         "references": [
           { "path": "./tsconfig.app.json" },
           { "path": "./tsconfig.node.json" }
         ]
       }
       ```

    6. Create `tsconfig.app.json`:
       ```json
       {
         "compilerOptions": {
           "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
           "target": "ES2020",
           "useDefineForClassFields": true,
           "lib": ["ES2020", "DOM", "DOM.Iterable"],
           "module": "ESNext",
           "skipLibCheck": true,
           "moduleResolution": "bundler",
           "allowImportingTsExtensions": true,
           "isolatedModules": true,
           "moduleDetection": "force",
           "noEmit": true,
           "jsx": "react-jsx",
           "strict": true,
           "noUnusedLocals": true,
           "noUnusedParameters": true,
           "noFallthroughCasesInSwitch": true,
           "noUncheckedSideEffectImports": true,
           "baseUrl": ".",
           "paths": {
             "@/*": ["./src/*"]
           }
         },
         "include": ["src"]
       }
       ```

    7. Create `tsconfig.node.json`:
       ```json
       {
         "compilerOptions": {
           "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
           "target": "ES2022",
           "lib": ["ES2023"],
           "module": "ESNext",
           "skipLibCheck": true,
           "moduleResolution": "bundler",
           "allowImportingTsExtensions": true,
           "isolatedModules": true,
           "moduleDetection": "force",
           "noEmit": true,
           "strict": true,
           "noUnusedLocals": true,
           "noUnusedParameters": true,
           "noFallthroughCasesInSwitch": true,
           "noUncheckedSideEffectImports": true
         },
         "include": ["vite.config.ts", "vitest.config.ts"]
       }
       ```

    8. Create `index.html` at project root:
       ```html
       <!doctype html>
       <html lang="en">
         <head>
           <meta charset="UTF-8" />
           <link rel="icon" type="image/svg+xml" href="/vite.svg" />
           <meta name="viewport" content="width=device-width, initial-scale=1.0" />
           <title>HyperCube Control</title>
         </head>
         <body>
           <div id="root"></div>
           <script type="module" src="/src/main.tsx"></script>
         </body>
       </html>
       ```

    9. Create `src/index.css` (Tailwind v4 CSS-first — no tailwind.config.js needed):
       ```css
       @import "tailwindcss";
       ```

    10. Create `src/main.tsx`:
        ```tsx
        import { StrictMode } from 'react';
        import { createRoot } from 'react-dom/client';
        import './index.css';
        import App from './App.tsx';

        createRoot(document.getElementById('root')!).render(
          <StrictMode>
            <App />
          </StrictMode>,
        );
        ```

    11. Create `src/App.tsx` (minimal shell — will be replaced in Plan 01-07/01-08):
        ```tsx
        export default function App() {
          return (
            <div className="min-h-screen bg-zinc-950 text-zinc-100 flex items-center justify-center">
              <h1 className="text-2xl font-bold">HyperCube Control</h1>
            </div>
          );
        }
        ```

    12. Create `src/test-setup.ts`:
        ```typescript
        import '@testing-library/jest-dom';
        import { afterEach } from 'vitest';
        import { cleanup } from '@testing-library/react';

        afterEach(() => {
          cleanup();
        });
        ```

    13. Create `.gitignore` if it doesn't exist (do NOT overwrite if it already has content):
        ```
        # Logs
        logs
        *.log
        npm-debug.log*

        # Runtime data
        pids
        *.pid
        *.seed
        *.pid.lock

        # Dependencies
        node_modules/
        .pnp
        .pnp.js

        # Build outputs
        dist/
        dist-ssr/
        *.local

        # Vite
        .vite/

        # TypeScript
        *.tsbuildinfo

        # Editor
        .DS_Store
        .vscode/
        *.swp
        *.swo

        # Test coverage
        coverage/
        ```
  </action>
  <verify>
    <automated>cd /Users/anshul/Anshul/Code/hyperspace-cube-control && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build` exits 0 and produces `dist/` folder. `npm run dev` starts without errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create directory skeleton and initialize shadcn/ui</name>
  <files>
    src/components/ui/.gitkeep,
    src/core/pipeline/.gitkeep,
    src/core/store/.gitkeep,
    src/core/wled/.gitkeep,
    src/plugins/inputs/.gitkeep,
    src/plugins/outputs/.gitkeep,
    src/plugins/mapping/.gitkeep,
    src/setup/.gitkeep,
    src/ui/.gitkeep,
    test/integration/.gitkeep,
    test/scenarios/.gitkeep,
    test/mocks/.gitkeep
  </files>
  <action>
    1. Create the full directory skeleton using `.gitkeep` files for empty dirs:
       - `src/components/ui/`
       - `src/core/pipeline/`
       - `src/core/store/`
       - `src/core/wled/`
       - `src/plugins/inputs/`
       - `src/plugins/outputs/`
       - `src/plugins/mapping/`
       - `src/setup/`
       - `src/ui/`
       - `test/integration/`
       - `test/scenarios/`
       - `test/mocks/`

    2. Run shadcn/ui init. This is an interactive CLI — use the non-interactive flags:
       ```bash
       cd /Users/anshul/Anshul/Code/hyperspace-cube-control
       npx shadcn@latest init -d
       ```
       If the `-d` (defaults) flag prompts for anything, answer:
       - Style: Default
       - Base color: Zinc
       - CSS variables: yes

    3. Add the initial shadcn components needed for Phase 1:
       ```bash
       npx shadcn@latest add button input label dialog card badge
       ```

    4. Verify `src/components/ui/button.tsx` exists after shadcn init.

    5. Verify `src/index.css` still contains `@import "tailwindcss"` after shadcn may have modified it. If shadcn overwrote it with PostCSS-style config, restore it to CSS-first:
       ```css
       @import "tailwindcss";
       /* shadcn/ui CSS variables */
       @layer base {
         :root {
           /* (shadcn vars will be here) */
         }
       }
       ```
       The key requirement: file starts with `@import "tailwindcss"` not with `@tailwind base` directives.
  </action>
  <verify>
    <automated>cd /Users/anshul/Anshul/Code/hyperspace-cube-control && ls src/components/ui/button.tsx && grep -q '@import "tailwindcss"' src/index.css && echo "OK"</automated>
  </verify>
  <done>
    `src/components/ui/button.tsx` exists. `src/index.css` contains `@import "tailwindcss"`. All directory skeleton folders exist. `npm run build` still exits 0.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
cd /Users/anshul/Anshul/Code/hyperspace-cube-control
npm run build
npx vitest run --dir src 2>&1 | head -20
ls src/components/ui/ | head -5
grep "@import" src/index.css
```

All commands should pass. `vitest run` may show "no tests found" — that is acceptable, not a failure.
</verification>

<success_criteria>
- `npm run build` exits 0
- `npm run dev` starts without errors
- `npx vitest run --dir src` runs without crashing (0 tests is fine)
- `src/index.css` contains `@import "tailwindcss"` (Tailwind v4 CSS-first)
- `src/components/ui/button.tsx` exists (shadcn/ui initialized)
- Full directory skeleton exists: `src/core/`, `src/plugins/`, `src/setup/`, `src/ui/`, `test/integration/`, `test/scenarios/`, `test/mocks/`
- TypeScript `strict: true` is set in tsconfig.app.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-wled-communication/01-01-SUMMARY.md`
</output>
